## 本轮结果（归档）

> **给助手的约束**：对用户**严禁**脚本名、引擎字段名、内部目录约定、运行机制；归档位置用**人话**一句即可（见 `VOICE.md` 第 2.1 节）。

### 须让用户知道

- 归档**放到哪了**（人能听懂的**一句**，避免罗列仓库路径）。
- **已经收尾**一句话。

### 不要对用户说

脚本名、内部目录约定、JSON 字段名、`.cjs`、路径（见 `VOICE.md`）。
