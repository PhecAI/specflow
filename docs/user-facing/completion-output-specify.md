## 本轮结果（需求说明）

> **给助手的约束（对用户可见）**  
> 只输出下面 **三个小节**，不要新增「原因」「你可直接打开的文件」「改完后我会…」等额外章节；总篇幅保持极简。完整禁用词与用语见 **`VOICE.md`**。

### 严禁（出现任一条即不合格）

- **任何**仓库路径、`ai-docs/`、`specify.md` / `plan.md`、`.cjs`、隐藏目录名、`.temp`。
- **任何**脚本/工具名、引擎/编排字段名（如 `confirm_*`、`ack`、`interaction`、`dispatch`、`AskQuestion`、`manage-state`、`gates` 等）。
- **任何**内部流程机制说明（如「按引擎约定」「门禁」「落盘」「再跑引擎」「状态同步」）。
- **任何**文档结构/编辑指令（Section、Clarification、CQ 代号、`[?]`、锚点）。

### 允许

- **业务内容**与 PRD 要点（可含飞书/外链，若用户需要留存）。
- **需求编号**：可说「当前需求编号是 xxx」，**不要**用路径指代。
- **阶段**用人话：如「需求说明已写好」「还差几件事要先定」「先不写技术方案、先改说明」。

### 1. 一句话结论

[约 30 字内：业务上达成了什么]

### 2. 还有没有要先定的事

若仍有阻塞：只列**简短标题**（不要长背景）。  
若没有：**写「没有，可以往下走」。**

### 3. 下一步

**只选一种**（按实际情况替换）：

- 还有待定：「请先完成上面几项，我再改需求说明。」
- 已定稿、可进入技术方案：「需求说明可以定稿了。要往下走时回复**继续**即可。」
- 用户明确表示**先不写技术方案、先改需求说明**：「好的，你先改需求说明，改好后回复**继续**或说一声。」（**不要**写弹窗、确认、脚本。）
