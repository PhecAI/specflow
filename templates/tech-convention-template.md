---
name: [规范名称，如 service-layer-standards]
description: [简短描述，如 某一分层或文件类型的核心编码规范]
globs: [匹配路径，如 src/<layer>/**/* 或 packages/<module>/**]
alwaysApply: false
---

# [技术栈名称/端名称] 技术规范 (Tech Conventions)

> 本规范由系统分析师逆向提取生成，作为本端代码生成的绝对约束基线。

## 1. 核心基建与技术栈 (Tech Stack & Core Libraries)
- **核心框架**: [例如 当前项目实际使用的框架/运行时]
- **核心生态**: 
  - 路由/入口: [例如 当前项目的路由或调度入口]
  - 状态/数据管理: [例如 当前项目的状态、缓存或持久化方案]
  - 外部调用: [例如 当前项目封装的网络、RPC、数据库或适配器]
  - 组件/基础设施: [例如 当前项目的组件库、中间件或共享工具]

## 2. 绝对红线与代码禁忌 (Red Lines & Forbidden Patterns)
- ❌ **[架构红线]**: [例如：禁止跨越当前分层直接调用底层依赖，必须通过约定入口]
- ❌ **[生态红线]**: [例如：禁止新增未批准的同类依赖，必须复用项目既有工具]
- ❌ **[安全红线]**: [例如：禁止硬编码任何密钥、Token 或环境变量]
- ✅ **[唯一推荐]**: [例如：同类文件必须沿用项目既有组织方式和命名模式]

## 3. 命名规约与代码风格 (Naming & Style Conventions)
- **文件与目录**: [例如：同一分层沿用存量目录命名与文件命名]
- **接口与模型**: [例如：入参与返回模型沿用项目既有后缀和结构]
- **静态检查**: [例如：遵循项目已有机械验证器或人工核对清单]

## 4. 黄金代码片段 (Golden Snippets - The "How-To")
[在这里提供 1-2 段从存量代码中提取的、最具代表性的标准代码片段，供大模型在后续开发中完美模仿。例如：一段标准的“带错误捕获和 Loading 状态的网络请求”该怎么写。]

```typescript
// 黄金代码片段示例 (请替换为真实项目代码)
```
