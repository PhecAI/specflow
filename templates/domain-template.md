# Domain: [业务领域名称，例如 User Auth / Payment / Order]

> **知识管理员 (Archive Agent) 维护指引**：
> 1. 本文档是该业务领域的“活体法典 (Living Documentation)”。
> 2. **统一语言**：必须使用业务域的通用语言，严禁出现具体技术实现细节（如 Redis, 数据库表名, Vue 组件）。
> 3. **冲突替换**：当新归档的需求与历史规则冲突或产生演进时，必须进行**原地重构 (Inline Refactor)** 替换或修改旧规则，**绝对禁止**在文末简单堆砌流水账。

---

## 1. 统一语言与核心实体 (Ubiquitous Language & Entities)

> **定义本领域内的核心专有名词、业务实体及关键状态枚举。**

- **[核心实体 A]**: [业务含义定义]
  - 关键属性: [如：订单包含实付金额、创建时间]
  - 状态机 (State Machine): [如：`PENDING(待支付)` -> `PAID(已支付)` -> `REFUNDING(退款中)`]

- **[业务黑话/专有名词 B]**: [业务含义解释，消除沟通歧义]

## 2. 核心业务规则 (Core Business Rules)

> **系统的长效约束条件。明确因果关系和边界。**

### 2.1 [子模块/业务流 A]
- **[规则/约束名称]**: [规则详细说明]
  - *前置条件 (Given)*: [例如：用户必须处于登录且已实名认证状态]
  - *流转限制 (Constraints)*: [例如：单笔退款金额不得超过订单总实付金额]
  - *异常流 (Exception)*: [例如：触发风控拦截时，冻结账户并通知客服]

### 2.2 [子模块/业务流 B]
- ...

## 3. 领域技术债与避坑指南 (Domain Tech Debt & Lessons)

> **记录在本领域的历次开发中发现的深坑、第三方限制，以及为了上线妥协留下的 TODO。作为下一次重构的靶子。**

- **[!] 避坑 (Lessons)**: [例如：微信支付接口在沙箱环境下不会触发异步回调，需手动 Mock]
- **[TODO] 技术债**: [例如：订单超时取消目前使用数据库轮询，未来数据量大时需重构为延迟队列]
